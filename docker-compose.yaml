version: "3.9"

services:
  caddy:
    image: caddy:2
    container_name: caddy-green-api
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./:/srv:ro
